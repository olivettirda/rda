## 추천 PR — #245

**도구**: 디지털육종 시뮬레이터 (rice_breeding 계열 — 출품 도구 1순위)
**분류**: [NEW]로 등록됐지만 사실상 [REFACTOR] — 직전 PR #244의 모듈 분리 결정을 46분 만에 뒤집어 인라인으로 통합
**머지일**: 2026-01-26 (08:02 — PR #244 머지 후 46분 뒤)
**변경 파일**: `rice_breeding_v4_16_prediction.html` 1개 (+1,137 -21)

**무엇이 문제였는지** (1줄, 비전공자도 이해 가능하게):
시뮬레이터 계산 코드를 부품처럼 여러 파일로 쪼개 뒀더니, HTML 한 파일을 받아 더블클릭으로 열어 쓰는 연구실 환경에서는 부품이 끼워지지 않아 작동하지 않았다.

**어떻게 바꿨는지** (1줄, 비전공자도 이해 가능하게):
쪼개 둔 모든 부품(JS 모듈 + Python 통계 엔진)을 HTML 파일 한 장 안에 다시 붙여 넣어서, 다른 파일 없이 그 HTML 하나만으로 시뮬레이터가 돌아가게 했다.

**왜 이 PR을 추천하는가** (3~5줄):
- 4건 중 background_selection이 2건이라 다양성이 필요한데, 이 PR은 1순위 도구(디지털육종 시뮬레이터)의 핵심 패키징 결정 사례여서 **도구 분포가 균형**잡힌다.
- 같은 날, 같은 Claude Code 세션에서 **4,783줄(#244 모듈화)을 만들었다가 46분 뒤(#245)에 통째로 우회**한 흔적이 PR 번호 순으로 명확히 남아 있어, "AI와 함께라서 분리 가설을 빠르게 검증·폐기했다"는 시행착오 메시지에 그대로 들어맞는다.
- "깔끔하게 분리하기 vs 한 파일로 주고받기"라는 갈등은 **현장 연구사가 매일 마주치는 선택**(논문 보낼 때 첨부 파일 1개? 폴더 압축?)이라, 솜여님이 한 줄 설명만 듣고도 "아 그래서 한 파일로 합친 거구나"로 직관할 수 있다.
- CLAUDE.md 모드별 머지 분기(#280)와 짝을 이루면 "AI 협업 결정의 빠른 번복"이라는 동일 주제를 **메타 규칙(#280) + 실제 코드 결정(#245)** 두 층위에서 보여줄 수 있다.

**대안 후보** (못 쓸 경우 백업, 2~3개):
- **PR #281 — 밀양 육성 11품종 전과정 데모 데이터 추가**: `rice_breeding_v5_0.html`(정확한 출품 파일)에 한국 육성 11품종 × 31유전자 데모셋을 얹은 PR. "전역 let 변수 직접 주입 시도의 한계 → 빌더 패턴으로 분리"라는 시행착오 흔적이 본문에 한 줄 있고, 농진청 출품답게 영호진미·백옥찰·다산 등 한국 품종 명시. 파일 diff는 +289 -0 순추가형이라 폐기 흔적은 약함.
- **PR #240 — Add pagination to offspring prediction results table**: `rice_breeding_v4_16_prediction.html`에서 후대 예측 결과가 너무 많아 한 번에 다 보여주면 느려지는 문제 → 20/50/100/500 페이지네이션. 비전공자에게 가장 단순한 한 줄("결과가 너무 많아서 페이지로 나눴다")이지만 시행착오 흔적은 가장 옅음.
- **PR #244 — Add BDSS modular JavaScript/Python integration layer**: #245의 짝 PR. "처음엔 깔끔하게 모듈 9개로 분리해서 만들었다(4,783줄 신규)"는 분리 시도 자체를 보여줌. #245 단독 대신 #244+#245 묶어서 "분리→통합" 두 컷으로 제시하는 변형 구도도 가능.
